// Placeholder for Firebase Crashlytics / Analytics initialization.
// Activate once firebase config files are added.
export function initTelemetry() {
  return;
}

export function captureError(_error: unknown) {
  return;
}

export function trackEvent(_name: string, _params?: Record<string, string | number | boolean | null>) {
  return;
}
