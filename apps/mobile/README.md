# RollerStat Mobile (Expo)

Android-first React Native app for the public RollerStat experience.

## Run locally

1. Start web backend from repo root:

```bash
npm run dev:web
```

2. In this folder:

```bash
npm install
npm run android
```

## Environment

Copy `.env.example` to `.env` and set values.

